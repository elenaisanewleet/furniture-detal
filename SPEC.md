# Сайт мастерской — техническое описание дизайна

Пакет для разработчика. Содержит прототип сайта, описание всех экранов, токены и правила поведения.

## Что в пакете

| Файл | Что это |
|---|---|
| `PROMPT.md` | Готовый промт: скопировать в чат с Claude Code |
| `SPEC.md` | Это описание: страницы, компоненты, токены, поведение |
| `design/` | Прототип на HTML/CSS/JS — 17 файлов, дизайн-референс |
| `tokens-detal73.css` | Целевая палитра и типографика |
| `design-system-readme.md` | Дизайн-система мастерской: тон, правила, компоненты |

## О файлах в `design/`

Это **дизайн-референсы, сделанные в HTML** — прототипы, показывающие задуманный вид и поведение,
а не продакшен-код для копирования. Задача — воспроизвести эти экраны в нормальной сборке
(рекомендуется Astro, как в `elenaisanewleet/furniture-site`), с её паттернами и структурой.
Открываются как есть, без сборки: `design/index.html` в браузере.

`model3d/` (three.js-модель мастерской: `workshop-model.js`, `three-d-stage.js`, `view.html`) идёт
отдельной папкой — распакуйте её внутрь `design/`, рядом с `masterskaya-3d.html`, иначе страницы
3D и каталога объектов откроются пустыми. В пакет не вошли черновики палитр (`pal-*.html`, `dir-*.html`, `palettes.html`, `index-dark.html`). Всё это лежит
в `elenaisanewleet/furniture-detal`, папка `ui_kits/master-site`.

## Точность

**High-fidelity.** Копирайт, структура страниц, SEO-разметка и логика формы — окончательные,
воспроизводить дословно. Палитра и типографика — «Деталь 73» (цинк, бумага, красный
сигнал, Golos Text + JetBrains Mono) — применены в прототипе, см. `design/site.css` и
`tokens-detal73.css`. Прежняя охристая палитра сохранена как `design/site-ochre.css` для сравнения;
в продакшене она не нужна. Шрифты в прототипе подключены с Google Fonts — в продакшене положить
локально, как в `furniture-site/public/fonts/`.

---

# Страницы

Тринадцать страниц, каждая — под свой кластер запросов, с перелинковкой между собой.

| Файл | Что это | Главный запрос |
|---|---|---|
| `index.html` | Главная: хаб услуг, заявка, склад, 3D | мебельная фурнитура Ульяновск |
| `furnitura-i-krepezh.html` | Фурнитура и крепёж | фурнитура Ульяновская область, крепёж партиями |
| `mebel-na-zakaz.html` | Мебель из дерева на заказ | мебель на заказ Ульяновская область |
| `remont-mebeli.html` | Ремонт и доработка мебели | ремонт мебели, замена фурнитуры |
| `metallicheskie-uzly.html` | Металлические узлы, кронштейны, пластины | кронштейны на заказ, пластина со шпилькой M8 |
| `raspil-i-prisadka.html` | Распил, присадка, сверление | распил по размерам, присадка |
| `zatochka.html` | Заточка ножей и инструмента | заточка ножей Ульяновск |
| `podbor-detali.html` | Определитель детали | как называется деталь мебели, минификс что это |
| `stanki.html` | Станки со схемами, заявка на операцию | услуги мастерской |
| `zakaz.html` | Заявка: форма, каналы, голосовое | заявка в мастерскую |
| `kontakty.html` | Контакты | мастерская село Архангельское |
| `masterskaya-3d.html` | 3D-модель мастерской, 12 ракурсов, 9 выносок | — |
| `katalog-3d.html` | Каталог объектов мастерской | — |

3D вынесена на отдельную страницу сознательно: на главной модель роняла плавность прокрутки.

## Главная

**Назначение.** Человек за пять секунд понимает, что здесь делают, и видит, куда написать.

Порядок блоков: шапка с телефоном → хиро (заголовок, подзаголовок, две кнопки) → шесть карточек
услуг (сетка 3×2, на мобильном 1 колонка) → блок «Определитель детали» → станочный парк со
ссылками-якорями → форма заявки → склад («уточните перед поездкой») → 3D → подвал.

## Определитель детали

Каталог наоборот: вход не по артикулу, а по тому, что человек видит. 12 деталей со схемами,
три входа:

1. **По месту** — дверца, ящик, купе, полка, корпус, низ мебели.
2. **По бытовому названию** — поиск по синонимам: «рельса» находит направляющую, «минификс» —
   эксцентриковую стяжку.
3. **По проблеме** — 37 симптомов.

У каждой детали: что она делает, что с ней обычно случается, что сфотографировать для опознания.
Кнопка «Заказать эту деталь» подставляет название в форму заявки.

Данные — `parts.js` (перенесены из `furniture-site`, `src/data/parts.ts`), логика — `finder.js`.

## Станки

Карточки станков с SVG-схемами. Схема прорисовывается по `stroke-dashoffset` при попадании
в вьюпорт (0.55s, `cubic-bezier(.4,0,.2,1)`, задержка 55ms на элемент). У каждой карточки якорь:
ссылка со страницы услуги открывает нужный станок раскрытым и запускает прорисовку.
Кнопка «Заказать эту работу» подставляет название станка в поле темы и прокручивает к форме.

---

# Форма заявки

Один блок, используется на всех страницах. Поля: имя (`f-name`), связь (`f-contact`),
задача (`f-task`, textarea), скрытая тема (`f-topic`).

**Номер заявки.** Формат `73-XXXX` (73 — код региона), генерируется при первой отправке,
живёт в `sessionStorage`, подставляется в текст сообщения и показывается клиенту после отправки,
чтобы клиент и оператор говорили об одной задаче. **В продакшене номер должен выдавать сервер** —
сейчас он клиентский и теряется при смене устройства.

**Каналы.**

- **WhatsApp** — собирает текст заявки и открывает `wa.me/79851982945?text=…`.
- **Telegram** — копирует текст в буфер и открывает чат: Telegram не принимает текст в ссылке.
  Бот заявок `@detal_zakaz_bot`; пока не заведён, флаг `BOT_READY=false` и заявки идут в личный
  Telegram `@elenaisanewleet`.
- **Позвонить** — `tel:+79851982945`.
- **Почта** — кнопка появляется автоматически, когда в `site.js` заполнена строка `var MAIL='';`.
  Пока адрес не задан, кнопка удаляется из DOM, чтобы не вести в пустоту.

**Голосовое сообщение.** `MediaRecorder`, до 3 минут, таймер `M:SS`, прослушивание и сохранение
файла (`zayavka-YYYY-MM-DD.webm`). Файл остаётся на устройстве — без бэкенда отправить его нельзя,
поэтому подсказка просит прикрепить запись в мессенджер. Если микрофон запрещён или браузер не
поддерживает запись, кнопка гаснет (opacity .45, disabled) и подсказка меняется на «надиктуйте
прямо в мессенджере».

**Что нужно доделать на бэкенде:** приём заявки, серверный номер, запись в хранилище, отправка
в Telegram-бота, загрузка голосового файла.

Текст письма (формат, воспроизводить дословно):

```
Заявка 73-4821 · <тема>
Имя: <имя>
Связь: <связь>
Задача: <задача>
```

Незаполненные поля подставляются как `—`.

---

# SEO

Это половина задачи, а не приложение к ней.

- Отдельная страница на услугу — под свой кластер запросов, с внутренними ссылками между собой.
- На каждой странице: `title`, `description`, `keywords`, `canonical`, Open Graph.
- Schema.org: `LocalBusiness` на главной, `Service` + `FAQPage` + `BreadcrumbList` на страницах услуг.
- Хлебные крошки на всех внутренних страницах.
- `sitemap.xml` (13 URL с приоритетами) и `robots.txt`.
- Гео-привязка: Ульяновская область, село Архангельское — в заголовках, разметке и текстах.
- Домен в canonical и sitemap сейчас `https://furniture-detal.vercel.app` — вынести в переменную.

---

# Токены

## Целевая палитра «Деталь 73»

Полный список — `tokens-detal73.css`. Ключевое:

| Роль | Значение |
|---|---|
| Чернила | `#14120f` `#23201b` `#3a352e` `#6b645a` `#9d968a` |
| Бумага | `#fafbfc` `#eef1f3` `#e3e8ec` `#d2d9de` |
| Оцинковка | `#e4e6e7` `#bcc1c4` `#8c9196` `#5c6266` |
| Сосна (только фотоматы) | `#f3e2c6` `#e3bc84` `#c08a3e` `#8a5e22` |
| Сигнал | `#f8ded8` `#d8402a` `#b02f1d` `#8f2517` |
| Линия | `rgb(20 18 15 / 0.14)` |

Правила, которые нельзя нарушать: **один сигнальный цвет на экран**, максимум два фона на страницу,
теней почти нет — глубина делается волосяной линией, радиусы мелкие (2/4/8/14px), `pill` только
у chip, uppercase только у моно-лейблов.

**Тёмная тема** поддерживается: `prefers-color-scheme` + `[data-theme]`. Сигнал в тёмной теме
поднимается по светлоте до `#f2614a` — `#d8402a` на почти-чёрном даёт 4.0:1 и не проходит
по контрасту.

## Типографика

- **Golos Text** 400–900 — интерфейс и заголовки. **JetBrains Mono** 400–700 — лейблы, шаги,
  размеры, коды деталей. Оба локально, переменные, кириллица отдельным подмножеством, всего 120 КБ.
  Не с Google Fonts: из России достаётся не всегда, и лишний домен в критическом пути тормозит
  первую отрисовку.
- Шкала: `--step-0` 14→16, `--step-1` 16→19, `--step-2` 18→24, `--step-3` 22→32, `--step-4` 28→44,
  `--step-5` 36→64, `--step-6` 44→88. `--display` 40→88. Все через `clamp()`.
- Интерлиньяж: `.9` / `.94` display, `1.12` heading, `1.55` body.
  Трекинг: `-.035em` display, `-.02em` heading, `.08em` eyebrow.
- Мера строки: 66ch проза, 34ch лид.

## Отступы и раскладка

База 4px: 4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96 / 128.
Вертикаль секции `clamp(3rem, 1.5rem + 6vw, 6rem)`. Контейнер 1200px (широкий 1440),
проза 66ch, желоб `clamp(20px, 0.6rem + 3vw, 64px)`.

## Формы, глубина, движение

Радиусы 2 / 4 / 8 / 14 / 999px — мелкие и «штампованные», как клеймо на металле.
Теней почти нет: `0 1px 2px rgb(20 18 15 / .08)` и `0 12px 32px -12px rgb(20 18 15 / .24)`
только для того, что действительно висит над страницей.

**Угловые метки** — фирменный приём: четыре уголка вместо рамки, 14px, 1.5px, цвет чернил.
Значат ровно одно: «сюда помещается предмет». Стоят на зоне загрузки, превью снимка и плитках
категорий — и больше нигде.

Движение механическое, ничего не пружинит: `cubic-bezier(.2,.8,.25,1)`,
длительности 90 / 140 / 240 / 420ms.

## Зоны нажатия

Минимум 44px, комфортно 52px, главная кнопка 60px. Шапка 60px на мобильном, 76px от 60rem;
липкий CTA 72px, на десктопе выключается.

---

# Ассеты

- **Фотографии.** Реальные снимки мастерской в публикацию не идут. В `furniture-site/public/photo/`
  пять обработанных `.webp` (хиро, помещение, верстак, точило, пресс) — можно использовать.
- **Чертежи.** `furniture-site/src/lib/drawings.ts` — 40 КБ SVG-чертежей деталей вместо фотографий.
  Перекрашиваются токенами `--draw-*`.
- **Текстуры.** `texture-grain.png`, `texture-mottle.png` — процедурные, не фотография.
- **3D-модель.** `elenaisanewleet/3d-model`, `workshop-model.js`: 12 ракурсов, 9 выносок по зонам.
- **Шрифты.** Четыре `.woff2` в `furniture-site/public/fonts/`.
- **Логотипа нет.** Вместо марки — слово **Мастерская** в Golos Text 800, трекинг `-.05em`.

# Контакты и факты

Николай — мастер. Елена +7 985 198 29 45 — оператор. Telegram `@elenaisanewleet`.
Бот заявок `@detal_zakaz_bot` (не заведён). Село Архангельское, Ульяновская область.

**Чего нет и что нельзя придумывать:** стаж, цены, сроки, гарантии, отзывы. Фактов по ним не было.
Наличие на складе — «уточните перед поездкой».
