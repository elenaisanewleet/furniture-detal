---
import Base from '~/layouts/Base.astro';
import Icon from '~/components/Icon.astro';
import { im, SIZES } from '~/data/images';
import { PRODUCTS, collectionByKey, formatThreshold } from '~/data/products';
import { site } from '~/data/site';

import { localeParams } from '~/i18n/config';
import type { Locale } from '~/i18n/config';

/** One page per language; the default locale keeps the bare URL. */
export function getStaticPaths() {
  return localeParams();
}

const { locale } = Astro.props as { locale: Locale };

const freeFrom = formatThreshold(site.shipping.freeFrom, locale);

/** Countries with Semers on a shelf, derived from the retailer list (Latvia + export markets). */
const countries = [...new Set(site.retailers.map((r) => r.country))];
const exportCountries = countries.filter((c) => c !== site.country);
const countryCount = countries.length;
const exportLine = exportCountries.join(', ');

/**
 * Qualitative milestones. Only 1888 is a documented date; nothing else carries a year.
 * Each stop is set as one enormous outlined word on the wall, with a photograph
 * drifting under it; `long` keys are set a size smaller so they stay on one line.
 */
const milestones = [
  {
    key: '1888',
    isYear: true,
    image: 'pastila-180',
    title: 'Belyov pastila is first sold',
    text: 'Merchant Amvrosy Prokhorov puts baked-apple pastila into commercial production in the town of Belyov. Antonovka apples, egg white, thin layers, patience.',
  },
  {
    key: 'The Baltics',
    image: 'pastila-texture',
    title: 'A recipe travels north',
    text: 'The same sour apple grows in orchards across the Baltics, and the same method follows it: a sweet that needs no sugar to make and no cold to keep.',
  },
  {
    key: 'Riga',
    image: 'process-oven',
    title: 'Semers starts baking pastila for the modern snack shelf',
    text: 'A bakery in Riga takes the loaf and cuts it down to a bar, bakes it into a meringue and presses it into a dessert square. The ingredient list stays two lines long.',
  },
  {
    key: 'Maxima & Barbora',
    image: 'hero-wide',
    title: 'On shelves across Latvia',
    text: 'App’Lite bars reach supermarket shelves and the online grocery basket at home.',
  },
  {
    key: 'Europe',
    image: 'lifestyle-desk',
    title: exportLine,
    text: 'Cases leave Riga for specialty and online stores in five more countries.',
  },
  {
    key: 'Today',
    image: 'tasting-box',
    title: 'This shop',
    text: 'Ordered here, packed in Riga, on its way in one to two business days. The recipe is still two ingredients long.',
  },
].map((m) => ({ ...m, long: m.key.length > 6 }));

/** The four shapes the loaf became, in the order they are told above. */
const shapes = [
  { slug: 'apple-bar-35g', label: 'The bar' },
  { slug: 'apple-meringue-35g', label: 'The meringue' },
  { slug: 'applite-baked-apple-dessert-50g', label: 'The dessert square' },
  { slug: 'flourless-apple-bar-50g', label: 'The flourless bar' },
].flatMap((s) => {
  const p = PRODUCTS.find((x) => x.slug === s.slug);
  return p ? [{ ...s, product: p }] : [];
});

/** One line per sub-brand; product names, weights and links come from the catalogue. */
const LINE_NOTES: Record<string, string> = {
  "App'Lite": 'The everyday line. Pastila cut to pocket size as a 35 g bar, and its thicker cut for a plate.',
  Flourless: 'The heartier bar: 50 g of baked apple pressed with whole berries, for the days when 35 g is not enough.',
  PastiLite: 'The same whipped apple, baked until it crackles. A meringue with no sugar to add.',
  'Belyov Pastila': 'The original loaf, in the shape it has had since 1888. The 100 g is the one to try first; the 180 g is for the table.',
  Belevini: 'Apple zephyr, the soft marshmallow of the east, set with agar. The one line in our range that does contain sugar, and the pack says so.',
};
const lines = site.brand.lines.map((brand) => {
  const products = PRODUCTS.filter((p) => p.brand === brand && p.collection !== 'gift-sets').sort((a, b) => a.order - b.order);
  const first = products[0];
  const col = first ? collectionByKey(first.collection) : undefined;
  return { brand, products, href: col ? `/shop/${col.key}/` : '/shop/', colName: col ? col.name : 'Shop', note: LINE_NOTES[brand] ?? '' };
});

const pad = (n: number) => String(n).padStart(2, '0');
---
<Base
  title="Our story: an 1888 pastila recipe, baked in Riga"
  description="The Semers story: a Belyov apple pastila recipe first sold in 1888, the sour Antonovka apple behind it, and a Riga bakery turning the loaf into bars."
  canonical="/story/"
  ogImage="pastila-block"
  breadcrumb={[{ name: 'Home', href: '/' }, { name: 'Our story', href: '/story/' }]}
>
  <!-- ============================== HERO: one enormous line, then air ============================== -->
  <section class="page-hero container story-hero" aria-labelledby="page-title">
    <p class="eyebrow">Our story · {site.locality}, {site.country}</p>
    <h1 id="page-title" class="story-hero__title" data-words>A sour apple, an old recipe and a bakery in <em>Riga</em>.</h1>
    <div class="story-hero__row">
      <div class="story-hero__copy" data-reveal>
        <p class="lead">Semers makes baked-apple snacks in Riga from a Belyov pastila recipe that was first sold in 1888: Antonovka apples, egg white, and a great deal of patience. This is where the recipe came from, why we have not touched it, and how a loaf turned into a bar.</p>
        <div class="cluster story-hero__cta">
          <a class="btn btn--lg" href="/shop/">Shop the range <Icon name="arrow" size={20} class="arrow" /></a>
          <a class="btn btn--lg btn--ghost" href="/how-its-made/">How it’s made</a>
        </div>
      </div>
      <div class="story-hero__media" data-reveal>
        <div class="pf story-hero__frame">
          <img src={im('pastila-slices').src} srcset={im('pastila-slices').srcset} sizes={im('pastila-slices').srcset && SIZES.half} alt={im('pastila-slices').alt} width={im('pastila-slices').width} height={im('pastila-slices').height} loading="eager" fetchpriority="high" decoding="async" data-parallax="0.12" />
        </div>
        <div class="story-hero__note"><strong>1888</strong><span>The year Belyov pastila was first sold. Two ingredients then, two ingredients now.</span></div>
      </div>
    </div>
    <div class="stats story-stats" data-stagger>
      <div class="stat"><strong>1888</strong><span>Belyov pastila first sold</span></div>
      <div class="stat"><strong data-count="2">2</strong><span>ingredients: baked apples, egg white</span></div>
      <div class="stat"><strong data-count="99">99%</strong><span>Antonovka apple in every bar</span></div>
      <div class="stat"><strong data-count={countryCount}>{countryCount}</strong><span>countries with Semers on a shelf</span></div>
    </div>
  </section>

  <!-- ============================== THE BELYOV TRADITION ============================== -->
  <section class="section container" aria-labelledby="belyov-title">
    <div class="split">
      <div class="pf pf--1x1 pf--honey" data-reveal>
        <img src={im('pastila-block').src} srcset={im('pastila-block').srcset} sizes={im('pastila-block').srcset && SIZES.third} alt={im('pastila-block').alt} width={im('pastila-block').width} height={im('pastila-block').height} loading="lazy" decoding="async" data-parallax="0.15" />
      </div>
      <div data-reveal>
        <p class="eyebrow">The Belyov tradition</p>
        <h2 id="belyov-title" data-words>It began in a small town with more apples than it could eat.</h2>
        <div class="prose mt-4">
          <p>Belyov is a small town in orchard country south of Moscow. Its apples are Antonovka: sour, aromatic, and impossible to keep fresh through a winter. Baking, whipping and drying them was the local answer, and in 1888 the merchant Amvrosy Prokhorov turned that answer into a business. Belyov pastila was sold for the first time.</p>
          <p>His method is the one we still use. Apples are baked whole until their sugars caramelise. The purée is whipped with egg white until it holds air, spread in thin sheets and dried for hours. The sheets are stacked, brushed with more of the foam, pressed and dried again. What comes out is light, layered, faintly tart, and keeps for months without a cellar.</p>
          <p>That last part is why the recipe travelled. A sweet that needs no sugar to make and no cold to keep suits the north, and pastila became a northern-European sweet: wherever sour apples grow and winters run long, someone was drying them into loaves.</p>
        </div>
        <ul class="cluster recipe" aria-label="The recipe">
          <li class="chip"><Icon name="apple" size={14} /> Baked apples</li>
          <li class="chip"><Icon name="egg" size={14} /> Egg white</li>
          <li class="chip"><Icon name="clock" size={14} /> Thin layers, slow drying</li>
        </ul>
        <div class="cluster mt-4">
          <a class="btn" href="/shop/pastila/">Shop Belyov pastila</a>
          <a class="link" href="/journal/what-is-pastila/">What pastila is, in full</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ============================== WHY ANTONOVKA ============================== -->
  <section class="section band" aria-labelledby="antonovka-title">
    <div class="container split split--rev">
      <div class="pf pf--4x5 pf--mint" data-reveal>
        <img src={im('apples-orchard').src} srcset={im('apples-orchard').srcset} sizes={im('apples-orchard').srcset && SIZES.third} alt={im('apples-orchard').alt} width={im('apples-orchard').width} height={im('apples-orchard').height} loading="lazy" decoding="async" data-parallax="0.15" />
      </div>
      <div data-reveal>
        <p class="eyebrow">Why Antonovka</p>
        <h2 id="antonovka-title" data-words>One apple makes the recipe possible. We have not found a second.</h2>
        <div class="prose mt-4">
          <p>Antonovka is a hardy, tart, green apple from the north. It carries more pectin and more acid than a supermarket dessert apple, and that is exactly what pastila needs. The pectin sets the whipped purée without gelatine. The acid keeps the finished sweetness honest, so a bar tastes of fruit rather than of sugar. And the aroma survives twelve hours of baking and drying, which very few apples manage.</p>
          <p>It grows well across the Baltics and the wider north, where a cold autumn sharpens it. Sweet apples make a flat, sticky pastila. Antonovka makes it sing.</p>
        </div>
        <ul class="ticks story-ticks">
          <li><Icon name="check" /> <span><strong>Pectin</strong> sets the foam, so there is no gelatine</span></li>
          <li><Icon name="check" /> <span><strong>Acid</strong> keeps the sweetness honest</span></li>
          <li><Icon name="check" /> <span><strong>Aroma</strong> survives the oven and the drying room</span></li>
        </ul>
        <div class="cluster">
          <a class="link" href="/journal/antonovka-apples/">Why Antonovka apples, in the journal</a>
          <a class="link" href="/how-its-made/">The full process</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ============================== FROM LOAF TO BAR ============================== -->
  <section class="section container" aria-labelledby="loaf-title">
    <div class="loaf">
      <div data-reveal>
        <p class="eyebrow">From loaf to bar</p>
        <h2 id="loaf-title" data-words>A loaf is for a table. We wanted something for a pocket.</h2>
        <div class="prose mt-4">
          <p>Belyov pastila is sold as a loaf. You slice it, share it and eat it with tea; it is a dessert with a ritual. In Riga we kept the loaf and asked a simpler question: what would this be if you could eat it on the way to the bus?</p>
          <p>The answer was App’Lite: the same whipped apple, cut to 35 g and sealed so it survives a bag and a lunchbox. Then PastiLite, the same foam baked until it crackles into a meringue. Then a thicker square for a plate, and a flourless bar pressed with whole berries for a long walk. Every one of them is the 1888 recipe in a different shape. None of them needed a new ingredient to get there.</p>
          <p>This shop came last. Until now you found us at Maxima, on Barbora and in stores across Europe; now the same cases can leave Riga for your door.</p>
        </div>
        <ul class="shapes" aria-label="The shapes the loaf became">
          {shapes.map((s, i) => (
            <li>
              <a href={`/products/${s.product.slug}/`}>
                <span class="shapes__n" aria-hidden="true">{pad(i + 1)}</span>
                <strong>{s.label}</strong>
                <span>{s.product.name} · {s.product.weightGrams} g</span>
              </a>
            </li>
          ))}
        </ul>
      </div>
      <div class="duo" data-stagger>
        <figure>
          <div class="pf pf--4x5">
            <img src={im('orchard-picking').src} srcset={im('orchard-picking').srcset} sizes={im('orchard-picking').srcset && SIZES.third} alt={im('orchard-picking').alt} width={im('orchard-picking').width} height={im('orchard-picking').height} loading="lazy" decoding="async" data-parallax="0.12" />
          </div>
          <figcaption>It starts in an orchard in autumn, when the apples are at their sharpest.</figcaption>
        </figure>
        <figure>
          <div class="pf pf--4x5">
            <img src={im('retail-boxes').src} srcset={im('retail-boxes').srcset} sizes={im('retail-boxes').srcset && SIZES.third} alt={im('retail-boxes').alt} width={im('retail-boxes').width} height={im('retail-boxes').height} loading="lazy" decoding="async" data-parallax="0.2" />
          </div>
          <figcaption>It ends as cases packed in Riga, on their way to a shop shelf or to you.</figcaption>
        </figure>
      </div>
    </div>
  </section>

  <!-- ============================== THE WALL: six stops as enormous outlined words ============================== -->
  <!--
    The timeline is a wall. Each stop is set as one huge outlined word or year,
    with its photograph drifting up over the lower half and a short paragraph
    beside it; sides alternate. The stop nearest the middle of the screen is
    lit in the accent (a class the script toggles; without it the wall is
    simply cream on the night).
  -->
  <section class="section wall" aria-labelledby="journey-title">
    <div class="container">
      <div class="wall__head">
        <p class="eyebrow">The journey</p>
        <h2 id="journey-title" data-words>Six stops between Belyov and your kitchen drawer.</h2>
        <p class="muted wall__note">We put a year only on the stop we can document. The rest are told in the order they happened.</p>
      </div>
      <ol class="wall__list">
        {milestones.map((m, i) => (
          <li class={`wall__stop ${i % 2 ? 'wall__stop--rev' : ''} ${m.long ? 'wall__stop--long' : ''}`} data-lit>
            {m.isYear
              ? <time class={`wall__year ${m.long ? 'wall__year--long' : ''}`} datetime={m.key}>{m.key}</time>
              : <span class={`wall__year ${m.long ? 'wall__year--long' : ''}`}>{m.key}</span>}
            <div class="wall__media" data-reveal>
              <div class="wall__frame">
                <img src={im(m.image).src} srcset={im(m.image).srcset} sizes={im(m.image).srcset && SIZES.half} alt={im(m.image).alt} width={im(m.image).width} height={im(m.image).height} loading="lazy" decoding="async" data-parallax="0.2" />
              </div>
            </div>
            <div class="wall__text" data-reveal>
              <span class="wall__n" aria-hidden="true">{pad(i + 1)} / {pad(milestones.length)}</span>
              <h3>{m.title}</h3>
              <p>{m.text}</p>
            </div>
          </li>
        ))}
      </ol>
    </div>
  </section>

  <!-- ============================== THE FAMILY, FULL BLEED, AND THE ONE SENTENCE ============================== -->
  <section class="fam" aria-labelledby="quote-title">
    <div class="fam__media">
      <img src={im('family-kitchen').src} srcset={im('family-kitchen').srcset} sizes="100vw" alt={im('family-kitchen').alt} width={im('family-kitchen').width} height={im('family-kitchen').height} loading="lazy" decoding="async" data-parallax="0.2" />
    </div>
    <div class="container fam__say" data-reveal>
      <p class="eyebrow" id="quote-title">In one sentence</p>
      <p class="fam__quote">“We did not invent a recipe. We found one that needed nothing, and built a bakery around leaving it alone.”</p>
      <p class="fam__by">The team at Semers, Riga. Semers is made by {site.brand.legalName}, {site.locality}, {site.country}.</p>
    </div>
  </section>

  <!-- ============================== WHAT WE BELIEVE: a ledger ============================== -->
  <!-- One row per rule: the numeral outlined in the margin, the rule as a display line, the reason beside it, a hairline between rows. -->
  <section class="section container" aria-labelledby="believe-title">
    <div class="section-head">
      <div>
        <p class="eyebrow">What we believe</p>
        <h2 id="believe-title" data-words>Three things we will not argue about.</h2>
      </div>
      <p class="muted" style="max-width:36ch">Not a mission statement. Three rules the recipe taught us, and the reasons we keep them.</p>
    </div>
    <ol class="creed" data-stagger>
      <li>
        <span class="creed__n" aria-hidden="true">01</span>
        <h3>Two ingredients</h3>
        <p>Baked apples and egg white. Berries, cinnamon and pine nuts may join them, because they are food. Sugar, syrups, flour, gelatine and flavourings may not, because the recipe never needed them.</p>
      </li>
      <li>
        <span class="creed__n" aria-hidden="true">02</span>
        <h3>Honest labels</h3>
        <p>The front of the pack says what the back of the pack says. No “natural flavouring”, no E-numbers, no claims we cannot stand behind. Where a product does contain sugar, as our Belevini zephyr does, the label says so plainly.</p>
      </li>
      <li>
        <span class="creed__n" aria-hidden="true">03</span>
        <h3>Apples first</h3>
        <p>We start from whole Antonovka apples, baked whole, skins on. When the apple is right, the recipe has nothing to fix, and most of our work is keeping it that way.</p>
      </li>
    </ol>
  </section>

  <!-- ============================== THE SUB-BRANDS ============================== -->
  <section class="section band" aria-labelledby="lines-title">
    <div class="container">
      <div class="section-head">
        <div>
          <p class="eyebrow">Five names on the packs</p>
          <h2 id="lines-title" data-words>The names on our packs, explained.</h2>
        </div>
        <a class="link" href="/shop/">Shop all products</a>
      </div>
      <ul class="lines" data-stagger>
        {lines.map((l) => (
          <li class="line">
            <h3>{l.brand}</h3>
            <div>
              <p>{l.note}</p>
              <div class="line__products">
                {l.products.map((p) => (
                  <a class="chip" href={`/products/${p.slug}/`}>{p.name} · {p.weightGrams} g</a>
                ))}
                <a class="chip chip--solid" href={l.href}>All {l.colName.toLowerCase()}</a>
              </div>
            </div>
          </li>
        ))}
      </ul>
      <p class="small muted mt-4">Gift sets, tasting boxes and the box of twelve live under <a href="/shop/gift-sets/">gift sets</a>. Questions about ordering are answered in the <a href="/faq/">FAQ</a>, or <a href="/contact/">write to us</a>.</p>
    </div>
  </section>

  <!-- ============================== CTA: the one lit room ============================== -->
  <section class="section container cta-sec" aria-labelledby="cta-title">
    <div class="cta-band" data-reveal>
      <p class="eyebrow">Taste the story</p>
      <h2 id="cta-title">Start with a bar. Or start with the loaf, as everyone did in 1888.</h2>
      <p>Parcels leave Riga in 1–2 business days and ship across the EU; orders over {freeFrom} ship free. Retailer, café or office? We pack cases as happily as parcels.</p>
      <div class="cluster mt-2">
        <a class="btn btn--paper" href="/shop/">Shop all products <Icon name="arrow" size={18} class="arrow" /></a>
        <a class="btn btn--ghost" href="/wholesale/">Wholesale &amp; B2B</a>
        <a class="link cta-band__link" href="/where-to-buy/">Where to buy</a>
      </div>
    </div>
  </section>
</Base>

<script>
  import { ScrollTrigger, reduced } from '~/scripts/motion';

  /*
   * The wall: the stop nearest the middle of the screen is lit. With reduced
   * motion the wall is complete as it is, every year outlined in cream.
   */
  if (!reduced) {
    for (const el of Array.from(document.querySelectorAll<HTMLElement>('[data-lit]'))) {
      ScrollTrigger.create({ trigger: el, start: 'top 62%', end: 'bottom 38%', toggleClass: 'is-lit' });
    }
  }
</script>

<style>
  /* ---------------------------------------------------------- parallax frames */
  /* A photograph that drifts needs to be taller than its window, or the drift shows the ground behind it. */
  .pf { position: relative; overflow: clip; border-radius: var(--r-xl); background: var(--bg-sunken); box-shadow: var(--shadow-2); }
  .pf img { position: absolute; left: 0; right: 0; top: -12%; width: 100%; height: 124%; object-fit: cover; }
  .pf--1x1 { aspect-ratio: 1; }
  .pf--4x5 { aspect-ratio: 4 / 5; }
  .pf--honey { box-shadow: var(--shadow-2), var(--glow-honey); }
  .pf--mint { box-shadow: var(--shadow-2), var(--glow-mint); }

  /* ---------------------------------------------------------- hero */
  .story-hero { padding-bottom: clamp(2rem, 5vw, 4rem); }
  .story-hero .story-hero__title { font-size: clamp(3rem, 2rem + 6.5vw, 8.5rem); line-height: 0.92; letter-spacing: -0.04em; max-width: 16ch; }
  .story-hero__title em { font-style: italic; font-weight: 400; color: var(--accent); font-variation-settings: 'opsz' 144, 'SOFT' 100, 'WONK' 1; }
  .story-hero__row { display: grid; gap: clamp(2rem, 5vw, 4rem); margin-top: clamp(2rem, 5vw, 3.5rem); align-items: end; }
  @media (min-width: 56rem) { .story-hero__row { grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr); } }
  .story-hero__copy { max-width: 40rem; }
  .story-hero__cta { margin-top: var(--sp-6); }
  .story-hero__media { position: relative; }
  .story-hero__frame { aspect-ratio: 3 / 2; box-shadow: var(--shadow-3); }
  @media (min-width: 56rem) { .story-hero__frame { aspect-ratio: 16 / 10; } }
  .story-hero__note { position: absolute; left: clamp(0.75rem, 3vw, 2rem); bottom: clamp(0.75rem, 3vw, 2rem); max-width: 16rem; display: grid; gap: 0.25rem; padding: 1rem 1.2rem; border-radius: var(--r-md); background: rgb(11 18 14 / 0.72); backdrop-filter: blur(14px); -webkit-backdrop-filter: blur(14px); border: 1px solid var(--line); font-size: var(--fs-sm); line-height: 1.3; color: var(--cream-2); }
  .story-hero__note strong { font-family: var(--font-display); font-size: var(--fs-2xl); font-weight: 500; letter-spacing: -0.03em; line-height: 1; color: var(--honey-500); }
  .story-stats { margin-top: clamp(2.5rem, 6vw, 4.5rem); padding-top: var(--sp-5); border-top: 1px solid var(--line); }

  /* ---------------------------------------------------------- tradition */
  .recipe { list-style: none; margin: var(--sp-5) 0 0; padding: 0; gap: 0.5rem; }
  .recipe .chip :global(svg) { color: var(--leaf-500); }

  /* ---------------------------------------------------------- antonovka */
  .story-ticks { margin: var(--sp-5) 0; }
  .story-ticks strong { font-weight: 600; color: var(--cream-0); }

  /* ---------------------------------------------------------- loaf to bar */
  .loaf { display: grid; gap: var(--sp-6); }
  @media (min-width: 56rem) { .loaf { grid-template-columns: 1fr 1fr; gap: clamp(2rem, 5vw, 4rem); align-items: center; } }
  .shapes { list-style: none; margin: var(--sp-5) 0 0; padding: 0; display: grid; gap: 0.6rem; grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .shapes a { position: relative; display: grid; gap: 0.1rem; height: 100%; padding: 1rem 1.1rem 1rem; border-radius: var(--r-md); background: var(--bg-raised); box-shadow: var(--shadow-1); text-decoration: none; transition: transform var(--dur) var(--ease-expo), box-shadow var(--dur); }
  .shapes a:hover { transform: translateY(-3px); box-shadow: var(--shadow-2), var(--glow-honey); }
  .shapes__n { position: absolute; right: 0.8rem; top: 0.5rem; font-family: var(--font-display); font-size: var(--fs-xl); font-weight: 400; color: transparent; -webkit-text-stroke: 1px var(--cream-3); letter-spacing: -0.04em; }
  .shapes strong { font-family: var(--font-display); font-size: var(--fs-lg); font-weight: 500; letter-spacing: -0.01em; color: var(--cream-0); }
  .shapes span:not(.shapes__n) { font-size: var(--fs-xs); color: var(--fg-3); }
  @media (max-width: 26rem) { .shapes { grid-template-columns: 1fr; } }
  .duo { display: grid; grid-template-columns: 1fr 1fr; gap: var(--sp-4); align-items: end; }
  .duo figure { margin: 0; display: grid; gap: var(--sp-3); }
  .duo figure:first-child { padding-bottom: clamp(2rem, 6vw, 4rem); }
  .duo figcaption { font-size: var(--fs-sm); color: var(--fg-3); max-width: 24ch; }
  .duo .pf { border-radius: var(--r-lg); }

  /* ---------------------------------------------------------- the wall */
  .wall { overflow: clip; padding-bottom: 0; }
  .wall__head { display: grid; gap: var(--sp-4); max-width: 60rem; margin-bottom: clamp(2rem, 6vw, 5rem); }
  .wall__head h2 { max-width: 16ch; }
  .wall__note { max-width: 44ch; }
  .wall__list { list-style: none; margin: 0; padding: 0; }
  /* `--overlap` is the share of the word's height the photograph is allowed to cover; most of every glyph stays in view. */
  .wall__stop { --year: clamp(5.5rem, 24vw, 15rem); --overlap: 0.12; position: relative; display: grid; gap: var(--sp-5); padding-block: clamp(2.5rem, 6vw, 5rem); border-top: 1px solid var(--line); }
  .wall__year { display: block; margin: 0; font-family: var(--font-display); font-size: var(--year); font-weight: 400; line-height: 0.85; letter-spacing: -0.05em; color: transparent; -webkit-text-stroke: 1px var(--cream-3); overflow-wrap: break-word; font-variation-settings: 'opsz' 144, 'SOFT' 30; transition: -webkit-text-stroke-color var(--dur-slow) var(--ease-out), filter var(--dur-slow) var(--ease-out); }
  .wall__stop--long { --year: clamp(3.5rem, 15vw, 9.5rem); }
  .wall__stop:first-child { --year: clamp(6rem, 26vw, 19rem); }
  .wall__stop.is-lit .wall__year { -webkit-text-stroke-color: var(--accent); filter: drop-shadow(0 0 22px rgb(255 90 60 / 0.35)); }
  .wall__media { position: relative; z-index: 1; margin-top: calc(var(--year) * -1 * var(--overlap)); }
  .wall__frame { position: relative; aspect-ratio: 4 / 3; overflow: clip; border-radius: var(--r-xl); background: var(--bg-sunken); box-shadow: var(--shadow-3); }
  .wall__frame img { position: absolute; left: 0; right: 0; top: -12%; width: 100%; height: 124%; object-fit: cover; }
  .wall__text { display: grid; gap: 0.6rem; align-content: end; max-width: 42ch; }
  .wall__n { font-size: var(--fs-xs); font-weight: 600; letter-spacing: 0.16em; color: var(--fg-3); font-variant-numeric: tabular-nums; }
  .wall__text h3 { font-size: var(--fs-2xl); letter-spacing: -0.02em; text-wrap: balance; }
  .wall__text p { margin: 0; color: var(--fg-2); font-size: var(--fs-lg); }
  @media (min-width: 56rem) {
    .wall__stop { grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); column-gap: clamp(2rem, 6vw, 6rem); align-items: end; }
    .wall__year { grid-column: 1 / -1; }
    .wall__stop { --overlap: 0.2; }
    .wall__stop--long { --overlap: 0.14; }
    .wall__media { grid-column: 1; grid-row: 2; max-width: 36rem; width: 100%; }
    .wall__text { grid-column: 2; grid-row: 2; padding-bottom: 1.25rem; }
    .wall__stop--rev .wall__year { text-align: right; }
    .wall__stop--rev .wall__media { grid-column: 2; justify-self: end; }
    .wall__stop--rev .wall__text { grid-column: 1; }
  }
  @media (prefers-reduced-motion: reduce) { .wall__year { transition: none; } }

  /* ---------------------------------------------------------- the family, full bleed */
  .fam { position: relative; isolation: isolate; overflow: clip; min-height: min(92svh, 56rem); display: grid; align-items: center; background: var(--night-950); }
  .fam__media { position: absolute; inset: -10% 0; z-index: 0; }
  .fam__media img { width: 100%; height: 100%; object-fit: cover; object-position: 60% 50%; opacity: 0.62; }
  .fam::before { content: ''; position: absolute; inset: 0; z-index: 1; background: linear-gradient(to right, rgb(6 10 8 / 0.92), rgb(6 10 8 / 0.6) 50%, rgb(6 10 8 / 0.2)), linear-gradient(to bottom, var(--night-900), transparent 22%, transparent 78%, var(--night-900)); }
  .fam__say { position: relative; z-index: 2; padding-block: clamp(5rem, 12vw, 9rem); max-width: 54rem; }
  .fam .eyebrow { color: var(--cream-2); }
  .fam .eyebrow::before { background: var(--honey-500); }
  .fam__quote { margin-top: var(--sp-5); font-family: var(--font-display); font-style: italic; font-weight: 400; font-size: var(--fs-3xl); line-height: 1.02; letter-spacing: -0.025em; max-width: 21ch; text-wrap: balance; color: var(--cream-0); font-variation-settings: 'opsz' 144, 'SOFT' 100, 'WONK' 1; }
  .fam__by { margin-top: var(--sp-5); font-size: var(--fs-sm); color: var(--cream-2); max-width: 40ch; }

  /* ---------------------------------------------------------- the creed: a ledger. Numeral in the margin, the rule as a display line, the reason beside it. */
  .creed { list-style: none; margin: 0; padding: 0; border-top: 1px solid var(--line); }
  .creed li { display: grid; grid-template-columns: 3.5rem minmax(0, 1fr); grid-template-areas: 'n h' 'n p'; gap: 0.6rem 1rem; align-items: start; padding-block: var(--sp-5); border-bottom: 1px solid var(--line); }
  .creed__n { grid-area: n; font-family: var(--font-display); font-size: var(--fs-2xl); font-weight: 400; line-height: 0.9; letter-spacing: -0.05em; color: transparent; -webkit-text-stroke: 1px var(--cream-3); font-variant-numeric: tabular-nums; transition: -webkit-text-stroke-color var(--dur), text-shadow var(--dur); }
  .creed h3 { grid-area: h; font-size: var(--fs-2xl); letter-spacing: -0.025em; line-height: 1; }
  .creed p { grid-area: p; margin: 0; color: var(--fg-2); max-width: 52ch; }
  .creed li:hover .creed__n { -webkit-text-stroke-color: var(--accent); text-shadow: 0 0 24px rgb(255 90 60 / 0.35); }
  @media (min-width: 48rem) {
    .creed li { grid-template-columns: 6rem minmax(0, 28ch) minmax(0, 1fr); grid-template-areas: 'n h p'; column-gap: clamp(1.5rem, 4vw, 3.5rem); padding-block: var(--sp-6); }
    .creed__n { font-size: var(--fs-3xl); line-height: 0.85; }
    .creed h3 { font-size: var(--fs-2xl); }
    .creed p { padding-top: 0.35rem; }
  }
  @media (prefers-reduced-motion: reduce) { .creed__n { transition: none; } }

  /* ---------------------------------------------------------- sub-brands */
  .lines { list-style: none; margin: 0; padding: 0; border-top: 1px solid var(--line); }
  .line { display: grid; gap: 0.6rem var(--sp-6); padding: var(--sp-5) 0; border-bottom: 1px solid var(--line); transition: background var(--dur); }
  @media (min-width: 56rem) { .line { grid-template-columns: 16rem 1fr; align-items: start; } }
  .line h3 { font-size: var(--fs-2xl); letter-spacing: -0.03em; }
  .line p { margin: 0; color: var(--fg-2); max-width: 56ch; }
  .line__products { display: flex; flex-wrap: wrap; gap: 0.4rem; margin-top: 0.8rem; }
  .line__products .chip { text-decoration: none; transition: border-color var(--dur-fast), background var(--dur-fast), color var(--dur-fast); }
  .line__products .chip:hover { border-color: var(--cream-1); color: var(--cream-0); }
  .line__products .chip--solid:hover { background: var(--cream-0); color: var(--night-900); }

  /* ---------------------------------------------------------- cta */
  /* The band's own padding does the spacing before the footer; the section adds none of its own. */
  .cta-sec { padding-bottom: var(--sp-6); }
  .cta-band__link { color: var(--night-900); border-color: var(--apple-600); }
  .cta-band__link:hover { color: var(--night-700); border-color: var(--night-900); }
</style>
